name: Deploy

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Use SHA tag from Build
        run: echo "a imagem é  ${{ env.IMAGE_TAG }}"
